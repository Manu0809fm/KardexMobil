name: Android Build
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4 # Actualizado a v4

      - name: Setup Gluon GraalVM
        uses: gluonhq/setup-graalvm@master
        with:
          graalvm: '22.1.0.1'
          jdk: 'java17'

      - name: Install Android SDK
        uses: android-actions/setup-android@v3 # Actualizado a v3

      - name: Build con Maven
        run: mvn clean gluonfx:build gluonfx:package -Pandroid

      - name: Guardar APK
        uses: actions/upload-artifact@v4 # Actualizado a v4
        with:
          name: KardexMobil-APK
          path: target/gluonfx/aarch64-linux-android/gvm/*.apk